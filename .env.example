# Database URL - PostgreSQL connection string (Vercel/Supabase integration)
STUDY_POSTGRES_PRISMA_URL="postgresql://postgres:postgres@localhost:5432/medcram?schema=public"
# Optional: overrides sslmode used by drizzle-kit (e.g. "no-verify")
DRIZZLE_SSLMODE="no-verify"

# Session security
AUTH_SESSION_SECRET="replace-with-a-long-random-string"

# Comma-separated admin emails
ADMIN_EMAILS="admin@medcram.app"
