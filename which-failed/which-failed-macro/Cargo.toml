[package]
name = "which-failed-macro"
version = "0.1.0"
edition = "2021"
description = "Procedural macro implementation for which-failed"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full", "parsing"] }
quote = "1"
proc-macro2 = "1"
